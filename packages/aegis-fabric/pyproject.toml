[project]
name = "aegis-fabric"
version = "0.1.0"
description = "AEGIS Data Fabric - Quality, Lineage, Catalog"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
    "structlog>=24.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
